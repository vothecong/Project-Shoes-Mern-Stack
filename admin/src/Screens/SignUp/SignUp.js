import React from 'react';
import Layout from '../../Components/Layout/Layout';

const SignUp = (props) => {
    return (
        <Layout>
            Product Screen
        </Layout>
    );
}

export default SignUp;
